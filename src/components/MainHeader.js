import React from 'react';
import MainMenu from "./MainMenu"

const MainHeader = ({ }) => (
    <header>
        <div className="main-header">
            <h1>Manage you own collection of books</h1>
            <a href="" className="btn btn-full">Try it out!</a>
        </div>
    </header>
);

export default MainHeader;